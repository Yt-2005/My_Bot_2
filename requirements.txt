# Core bot
python-telegram-bot[job-queue]>=20.0
python-dotenv>=1.0.0
psycopg[binary]>=3.0.0

# Image processing (lightweight — no torch/GPU needed)
Pillow>=10.0.0
httpx>=0.24.0

# Web / dashboard
Flask>=3.0.0
aiohttp>=3.9.0