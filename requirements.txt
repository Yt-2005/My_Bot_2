# Core bot
python-telegram-bot[job-queue]>=20.0
python-dotenv>=1.0.0
psycopg[binary]>=3.0.0

# Image processing (lightweight — no torch/GPU needed)
Pillow>=10.0.0
httpx>=0.24.0

# Web / dashboard
Flask>=3.0.0
aiohttp>=3.9.0

# AI
groq>=0.9.0

# Upscaling (pick ONE based on what upscale_engine provides)
# Option A — if upscale_engine is your own file, nothing to add here
# Option B — if it's a lightweight waifu2x/Real-ESRGAN wrapper:
# realesrgan        ← heavy, needs torch
# Option C — lightweight alternative (no GPU):
# super-image>=0.1.3